<?xml version="1.0" encoding="utf-8"?>
<!-- AVALON custom main manifest — adds REQUEST_INSTALL_PACKAGES for the self-updater -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-feature android:glEsVersion="0x00030000" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/app_icon"
        android:allowBackup="true"
        android:supportsRtl="true"
        tools:targetApi="31">

        <activity android:name="com.bigfoot404.avalon.AvalonLauncherActivity"
                  android:theme="@style/UnityThemeSelector"
                  android:launchMode="singleTask"
                  android:exported="true"
                  android:configChanges="mcc|mnc|locale|layoutDirection|uiMode|screenLayout|screenSize|smallestScreenSize|orientation|keyboard|keyboardHidden|navigation|density|fontScale|touchscreen">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data android:name="unityplayer.UnityActivity" android:value="true" />
        </activity>
    </application>
</manifest>
