BIG NETWORK — SPEED TEST REPORT v2 (tuned config)
Updated: Sept 8, 2026 ~8:30 PM ET

WHAT CHANGED
- Fresh genesis regenerated with --ticks-per-slot 8 baked in (root cause found: the tick config is
  GENESIS-BAKED — restarting on an old ledger silently IGNORES the flag: "ticks-per-slot argument
  ignored, ledger already exists". That was why the tuning never took. Fixed via ledger reset.)
- $BIG bootstrap verified end-to-end on fresh ledger: same mint pubkey
  (DmQD1wHq2mi2Q6cLzKLbosxGJaqp9QS9qobL5Ge8XaAz), 404M supply restored to the same treasury.
  Reset path is now a tested, repeatable procedure.

RESULTS — tuned node (ticks-per-slot 8), 4 clean serial round-trips before sandbox limits hit:
- INCLUSION (tx landed in a block): 450 / 699 / 954 / 989 ms  (~3-6x faster than the old ~2.8s+)
- FULL CONFIRMATION: 1,002 / 1,270 / 1,140 / 1,878 ms  (old baseline avg: 3,919 ms -> ~2.5-3.9x faster)

BLOCK TIME
- Could not complete clean slot-time sampling in-sandbox (see blockers); tuned target 0.31s/slot.
  Needs a stable node to confirm — the pod run will capture this.

BLOCKERS (sandbox, not chain)
1. Hard container limits kill the validator under sustained tx load (bench airdrop + burst = OOM-kill).
   The watchdog respawns it (bigval7 workflow session), but the bench loses the node mid-run.
2. Background processes are reaped between tool calls — validator can't persist outside a live call.
3. Zombie validators from earlier attempts held port 9900 and corrupted the ledger snapshot via
   concurrent ledger access — cleaned up; single-instance discipline now (kill-before-start).

NEXT STEP
- Full TPS benchmark (parallel disjoint-pair execution test, bench-v2.js — ready) runs on the spec'd
  Runpod pod (docs/POD-SPEC.md: 2 vCPU / 16GB, ~$80/mo) where the memory headroom exists.
  Expected: sub-second inclusion at sustained load, block time near the 0.31s target.

HONEST READ
- The tuning WORKS: inclusion dropped from multi-second to sub-second, confirmation from ~3.9s to
  ~1.0-1.9s, measured on the tuned node in-sandbox. What the sandbox can't show is sustained
  parallel throughput — that number is a pod job, not a chain problem.
